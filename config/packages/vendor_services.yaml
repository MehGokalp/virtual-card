services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    VirtualCard\Vendor\Bear\Service\Router:
        arguments:
            - '%env(VENDOR_BEAR_WS)%'

    VirtualCard\Vendor\Lion\Service\Create:
        arguments:
            - '%env(VENDOR_LION_WS)%'

    VirtualCard\Vendor\Rhino\Service\Create:
        arguments:
            - '%env(VENDOR_RHINO_WS)%'
